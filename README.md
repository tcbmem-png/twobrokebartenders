# Two Broke Bartenders recovered static site

Recovered from the live Vercel deployment.

Files:
- `index.html` — static landing page
- `logo.png` — small circular favicon/nav/hero logo
- `og-image.png` — large social preview image referenced by Open Graph/Twitter metadata

Deployment:
- Vercel should use this repo as a static site.
- No build command is required.
- Output directory should be left blank/default.
